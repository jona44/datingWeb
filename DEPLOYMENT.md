# Deployment Guide - DatingApp

This repository is configured for Railway deployment.

Use `RAILWAY_DEPLOYMENT.md` for the Railway checklist, required services,
environment variables, and deploy steps.
